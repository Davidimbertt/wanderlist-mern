{
  "name": "wanderlist",
  "version": "1.0.0",
  "private": true,
  "description": "A full-stack MERN travel itinerary planner.",
  "scripts": {
    "dev": "concurrently --kill-others --names BACKEND,FRONTEND --prefix-colors cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "keywords": [
    "mern",
    "travel",
    "itinerary",
    "mongodb",
    "react"
  ],
  "author": "David Imbert",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}